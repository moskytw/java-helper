It is for my dear MIS classmates. :D

Compile the sources:

    cd java-help
    javac -d class sources/*
    javac -d class -cp class test/*

Execute the test cases:

    java -cp class testopen
    java -cp class testscan

Find usages of this package in `test` folder.
